{
  "name": "my-playwright-project",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "npx playwright test",
    "test:e2e": "npx playwright test tests/e2e",
    "test:api": "npx playwright test tests/api",
    "test:db": "npx playwright test tests/db",
    "test:allure": "npx rimraf allure-results && npx playwright test --reporter=list,allure-playwright tests && node ./scripts/preserve-allure-history.js && npx allure generate allure-results --clean -o allure-report && npx allure open allure-report",
    "test:e2e:allure": "npx rimraf allure-results && npx playwright test tests/e2e --reporter=list,allure-playwright && node ./scripts/preserve-allure-history.js && npx allure generate allure-results --clean -o allure-report && npx allure open allure-report",
    "test:api:allure": "npx rimraf allure-results && npx playwright test tests/api --reporter=list,allure-playwright && node ./scripts/preserve-allure-history.js && npx allure generate allure-results --clean -o allure-report && npx allure open allure-report",
    "test:db:allure": "npx rimraf allure-results && npx playwright test tests/db --reporter=list,allure-playwright && node ./scripts/preserve-allure-history.js && npx allure generate allure-results --clean -o allure-report && npx allure open allure-report",
    "test:headed": "npx playwright test --headed",
    "test:debug": "npx playwright test --debug",
    "allure:zip-history": "node ./scripts/zip-allure-history.js",
    "allure:unzip-history": "node ./scripts/unzip-allure-history.js"
  },
  "devDependencies": {
    "@aws-sdk/client-s3": "^3.0.0",
    "@playwright/test": "^1.62.0",
    "@types/pg": "^8.20.0",
    "allure-commandline": "^2.23.0",
    "allure-playwright": "^2.0.0",
    "pg": "^8.22.0",
    "rimraf": "^5.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^7.0.2"
  }
}
